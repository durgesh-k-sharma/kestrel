# M0 Scaffold Implementation Plan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Create the Kestrel project scaffold — GitHub repo, Cargo workspace with 9 crates, CI/CD, and a hello-world binary that prints the project name and version.

**Architecture:** Cargo workspace with 9 crates mirroring compiler phases. Each crate is independently testable. CI/CD via GitHub Actions with comprehensive checks.

**Tech Stack:** Rust 1.96.0, Cargo, GitHub Actions, cargo-llvm-cov, cargo-fmt, cargo-clippy

---

### Task 1: Create GitHub repository

**Objective:** Create the `durgesh-k-sharma/kestrel` GitHub repo with proper settings.

**Files:**
- Create: GitHub repo via `gh repo create`

**Step 1: Create the repo**

Run:
```bash
cd /home/durgesh/projects/rit
gh repo create durgesh-k-sharma/kestrel --public --description "Kestrel — A C11 compiler written in Rust" --homepage "https://github.com/durgesh-k-sharma/kestrel"
```

Expected: Repository created at https://github.com/durgesh-k-sharma/kestrel

**Step 2: Verify repo exists**

Run:
```bash
gh repo view durgesh-k-sharma/kestrel
```

Expected: Shows repo details including name, description, URL.

**Step 3: Commit**

```bash
git add -A
git commit -m "chore: create GitHub repository"
git push origin main
```

---

### Task 2: Initialize Cargo workspace

**Objective:** Create the Cargo workspace with all 9 crates and proper structure.

**Files:**
- Create: `Cargo.toml` (workspace root)
- Create: `Cargo.lock` (auto-generated)
- Create: `kestrel_common/Cargo.toml`, `kestrel_common/src/lib.rs`
- Create: `kestrel_lexer/Cargo.toml`, `kestrel_lexer/src/lib.rs`
- Create: `kestrel_preprocessor/Cargo.toml`, `kestrel_preprocessor/src/lib.rs`
- Create: `kestrel_parser/Cargo.toml`, `kestrel_parser/src/lib.rs`
- Create: `kestrel_sema/Cargo.toml`, `kestrel_sema/src/lib.rs`
- Create: `kestrel_ir/Cargo.toml`, `kestrel_ir/src/lib.rs`
- Create: `kestrel_opt/Cargo.toml`, `kestrel_opt/src/lib.rs`
- Create: `kestrel_codegen/Cargo.toml`, `kestrel_codegen/src/lib.rs`
- Create: `kestrel_driver/Cargo.toml`, `kestrel_driver/src/main.rs`
- Create: `kestrel/Cargo.toml`, `kestrel/src/lib.rs` (top-level re-export crate)

**Step 1: Create workspace root Cargo.toml**

```toml
[workspace]
members = [
    "kestrel",
    "kestrel_common",
    "kestrel_lexer",
    "kestrel_preprocessor",
    "kestrel_parser",
    "kestrel_sema",
    "kestrel_ir",
    "kestrel_opt",
    "kestrel_codegen",
    "kestrel_driver",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.96.0"
license = "MIT OR Apache-2.0"
authors = ["Durgesh Kumar Sharma <durgesh-k-sharma>"]
repository = "https://github.com/durgesh-k-sharma/kestrel"
description = "Kestrel — A C11 compiler written in Rust"
keywords = ["compiler", "c11", "rust", "codegen", "ssa"]
categories = ["compilers", "development-tools"]

[workspace.dependencies]
# Shared dependencies go here (added as needed)

[profile.release]
opt-level = 2
lto = true
```

**Step 2: Create each crate's Cargo.toml and lib.rs/main.rs**

Each library crate gets:
```toml
[package]
name = "kestrel_<name>"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description.workspace = true

[dependencies]
kestrel_common = { path = "../kestrel_common" }
```

Each crate's `src/lib.rs` starts with:
```rust
//! <Crate description>
//!
//! Part of the Kestrel C11 compiler project.

pub const fn crate_name() -> &'static str {
    env!("CARGO_PKG_NAME")
}
```

The `kestrel_driver/Cargo.toml` adds:
```toml
[[bin]]
name = "kestrel"
path = "src/main.rs"

[dependencies]
kestrel_common = { path = "../kestrel_common" }
```

The `kestrel_driver/src/main.rs`:
```rust
fn main() {
    println!("Kestrel C11 compiler v{}", env!("CARGO_PKG_VERSION"));
}
```

The top-level `kestrel/Cargo.toml`:
```toml
[package]
name = "kestrel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Kestrel — A C11 compiler written in Rust (top-level re-export crate)"

[dependencies]
kestrel_common = { path = "../kestrel_common" }
kestrel_lexer = { path = "../kestrel_lexer" }
kestrel_preprocessor = { path = "../kestrel_preprocessor" }
kestrel_parser = { path = "../kestrel_parser" }
kestrel_sema = { path = "../kestrel_sema" }
kestrel_ir = { path = "../kestrel_ir" }
kestrel_opt = { path = "../kestrel_opt" }
kestrel_codegen = { path = "../kestrel_codegen" }
```

The top-level `kestrel/src/lib.rs`:
```rust
//! Kestrel — A C11 compiler written in Rust.
//!
//! This crate re-exports all Kestrel compiler library crates.

pub use kestrel_common;
pub use kestrel_lexer;
pub use kestrel_preprocessor;
pub use kestrel_parser;
pub use kestrel_sema;
pub use kestrel_ir;
pub use kestrel_opt;
pub use kestrel_codegen;
```

**Step 3: Verify workspace builds**

Run:
```bash
cargo build --workspace
```

Expected: All crates compile successfully.

**Step 4: Verify hello world**

Run:
```bash
cargo run --bin kestrel
```

Expected: Prints "Kestrel C11 compiler v0.1.0"

**Step 5: Commit**

```bash
git add -A
git commit -m "chore: initialize Cargo workspace with 9 crates"
git push origin main
```

---

### Task 3: Set up CI/CD — ci.yml

**Objective:** Create the main CI workflow that runs on every push/PR.

**Files:**
- Create: `.github/workflows/ci.yml`

**Step 1: Create CI workflow**

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo check --workspace --all-targets

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo test --workspace --all-targets

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --workspace --all-targets -- -D warnings

  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo fmt --all -- --check
```

**Step 2: Verify CI passes**

Push and check the Actions tab on GitHub. All 4 jobs should pass.

**Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "ci: add CI workflow (check, test, clippy, fmt)"
git push origin main
```

---

### Task 4: Set up release workflow

**Objective:** Create the release workflow that builds binaries and publishes to crates.io.

**Files:**
- Create: `.github/workflows/release.yml`

**Step 1: Create release workflow**

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true

  build-binaries:
    name: Build ${{ matrix.target }}
    needs: create-release
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-latest
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
          - target: x86_64-pc-windows-msvc
            os: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - name: Build
        run: cargo build --release --bin kestrel --target ${{ matrix.target }}
      - name: Package
        shell: bash
        run: |
          cd target/${{ matrix.target }}/release
          if [[ "${{ matrix.target }}" == *"windows"* ]]; then
            zip kestrel-${{ matrix.target }}.zip kestrel.exe
          else
            tar czf kestrel-${{ matrix.target }}.tar.gz kestrel
          fi
      - name: Upload
        uses: softprops/action-gh-release@v2
        with:
          files: target/${{ matrix.target }}/release/kestrel-${{ matrix.target }}.*

  publish-crates:
    name: Publish to crates.io
    needs: create-release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Publish
        run: cargo publish --workspace --token ${{ secrets.CARGO_TOKEN }}
```

**Step 2: Commit**

```bash
git add .github/workflows/release.yml
git commit -m "ci: add release workflow (binaries + crates.io)"
git push origin main
```

---

### Task 5: Set up documentation workflow

**Objective:** Create the docs workflow that builds and publishes cargo doc to GitHub Pages.

**Files:**
- Create: `.github/workflows/docs.yml`
- Create: `.github/workflows/pages.yml` (or combine into docs.yml)

**Step 1: Create docs workflow**

```yaml
name: Docs

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo doc --workspace --no-deps
      - name: Add index redirect
        run: echo '<meta http-equiv="refresh" content="0;url=kestrel/index.html">' > target/doc/index.html
      - uses: actions/upload-pages-artifact@v3
        with:
          path: target/doc

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/deploy-pages@v4
        id: deployment
```

**Step 2: Enable GitHub Pages**

Run:
```bash
gh api repos/durgesh-k-sharma/kestrel/pages -X POST -f source.branch=main -f source.path=/docs
```

**Step 3: Commit**

```bash
git add .github/workflows/docs.yml
git commit -m "ci: add docs workflow (cargo doc -> GitHub Pages)"
git push origin main
```

---

### Task 6: Set up coverage workflow

**Objective:** Create the coverage workflow using cargo-llvm-cov.

**Files:**
- Create: `.github/workflows/coverage.yml`

**Step 1: Create coverage workflow**

```yaml
name: Coverage

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: taiki-e/install-action@cargo-llvm-cov
      - run: cargo llvm-cov --workspace --all-features --lcov --output-path lcov.info
      - uses: codecov/codecov-action@v4
        with:
          files: lcov.info
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
```

**Step 2: Commit**

```bash
git add .github/workflows/coverage.yml
git commit -m "ci: add coverage workflow (cargo-llvm-cov)"
git push origin main
```

---

### Task 7: Add project documentation files

**Objective:** Create README.md, LICENSE files, CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md, and .gitignore.

**Files:**
- Create: `README.md`
- Create: `LICENSE-MIT`
- Create: `LICENSE-APACHE`
- Create: `CONTRIBUTING.md`
- Create: `CODE_OF_CONDUCT.md`
- Create: `SECURITY.md`
- Create: `.gitignore`
- Create: `rustfmt.toml`
- Create: `clippy.toml`

**Step 1: Create README.md**

```markdown
# Kestrel

[![CI](https://github.com/durgesh-k-sharma/kestrel/actions/workflows/ci.yml/badge.svg)](https://github.com/durgesh-k-sharma/kestrel/actions/workflows/ci.yml)
[![Docs](https://github.com/durgesh-k-sharma/kestrel/actions/workflows/docs.yml/badge.svg)](https://github.com/durgesh-k-sharma/kestrel/actions/workflows/docs.yml)
[![License](https://img.shields.io/badge/license-MIT%20OR%20Apache--2.0-blue.svg)](LICENSE-MIT)
[![crates.io](https://img.shields.io/crates/v/kestrel.svg)](https://crates.io/crates/kestrel)

**Kestrel** is a C11 compiler written in Rust. It features:

- **Multi-backend**: x86-64, ARM64 (AArch64), RISC-V
- **SSA-based IR** with 11 optimization passes
- **Integrated C preprocessor** (no external cpp needed)
- **Library + CLI** — use as a Rust library or command-line tool
- **Comprehensive testing** — unit, integration, differential, fuzzing

## Status

🚧 Active development — [M0: Scaffold](docs/plans/2026-06-04-m0-scaffold.md)

## Quick Start

```bash
# Build
cargo build --release --bin kestrel

# Run
cargo run --bin kestrel

# Test
cargo test --workspace
```

## Architecture

```
Source → Preprocessor → Lexer → Parser → Semantic Analysis → AST → SSA IR → Optimizations → Backend
```

## License

Dual-licensed under [MIT](LICENSE-MIT) or [Apache 2.0](LICENSE-APACHE).
```

**Step 2: Create LICENSE files**

`LICENSE-MIT`: Standard MIT license text with copyright "2026 Durgesh Kumar Sharma"
`LICENSE-APACHE`: Standard Apache 2.0 license text

**Step 3: Create .gitignore**

```
/target
**/*.rs.bk
Cargo.lock
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
```

**Step 4: Create rustfmt.toml**

```toml
edition = "2024"
max_width = 100
tab_spaces = 4
```

**Step 5: Create clippy.toml**

```toml
cognitive-complexity-threshold = 30
```

**Step 6: Create CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md**

Standard templates (keep concise for now, expand later).

**Step 7: Commit**

```bash
git add -A
git commit -m "docs: add README, LICENSE, CONTRIBUTING, CODE_OF_CONDUCT, SECURITY, rustfmt, clippy"
git push origin main
```

---

### Task 8: Set up GitHub labels

**Objective:** Create the canonical triage labels on the GitHub repo.

**Files:** (GitHub repo labels via gh CLI)

**Step 1: Create labels**

Run:
```bash
gh label create "bug" --description "Something isn't working" --color "d73a4a" --repo durgesh-k-sharma/kestrel
gh label create "enhancement" --description "New feature or request" --color "a2eeef" --repo durgesh-k-sharma/kestrel
gh label create "documentation" --description "Improvements or additions to documentation" --color "0075ca" --repo durgesh-k-sharma/kestrel
gh label create "good first issue" --description "Good for newcomers" --color "7057ff" --repo durgesh-k-sharma/kestrel
gh label create "help wanted" --description "Extra attention is needed" --color "008672" --repo durgesh-k-sharma/kestrel
gh label create "milestone" --description "Tracks a milestone (M0-M10)" --color "fbca04" --repo durgesh-k-sharma/kestrel
gh label create "compiler-frontend" --description "Lexer, parser, preprocessor, semantic analysis" --color "c5def5" --repo durgesh-k-sharma/kestrel
gh label create "compiler-backend" --description "Code generation, register allocation" --color "bfd4f2" --repo durgesh-k-sharma/kestrel
gh label create "optimization" --description "SSA optimization passes" --color "e4e669" --repo durgesh-k-sharma/kestrel
gh label create "testing" --description "Tests, fuzzing, differential testing" --color "cc317c" --repo durgesh-k-sharma/kestrel
```

**Step 2: Verify labels**

Run:
```bash
gh label list --repo durgesh-k-sharma/kestrel
```

Expected: 10 labels listed.

---

### Task 9: Verify complete scaffold

**Objective:** End-to-end verification that the entire scaffold works.

**Step 1: Full build**

Run:
```bash
cargo build --workspace --release
```

Expected: All crates compile.

**Step 2: Full test**

Run:
```bash
cargo test --workspace
```

Expected: All tests pass (each crate's `crate_name()` test).

**Step 3: Clippy**

Run:
```bash
cargo clippy --workspace --all-targets -- -D warnings
```

Expected: No warnings.

**Step 4: Format check**

Run:
```bash
cargo fmt --all -- --check
```

Expected: All files properly formatted.

**Step 5: Hello world**

Run:
```bash
cargo run --bin kestrel
```

Expected: Prints "Kestrel C11 compiler v0.1.0"

**Step 6: Final commit and tag**

```bash
git add -A
git commit -m "chore: M0 scaffold complete — workspace, CI/CD, docs, labels"
git tag v0.1.0-m0
git push origin main --tags
```

---

## Verification Checklist

- [ ] GitHub repo created at `durgesh-k-sharma/kestrel`
- [ ] Cargo workspace with 9 crates builds successfully
- [ ] `cargo run --bin kestrel` prints version
- [ ] CI workflow passes (check, test, clippy, fmt)
- [ ] Release workflow created
- [ ] Docs workflow created
- [ ] Coverage workflow created
- [ ] README.md with badges
- [ ] LICENSE-MIT and LICENSE-APACHE
- [ ] CONTRIBUTING.md, CODE_OF_CONDUCT.md, SECURITY.md
- [ ] .gitignore, rustfmt.toml, clippy.toml
- [ ] 10 GitHub labels created
- [ ] All changes pushed to main
